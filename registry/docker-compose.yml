---
version: '3'

services:
  registry:
    image: registry:2
    container_name: local-registry
    restart: unless-stopped    
    ports:
      - 5000:5000
    networks:
      - registry

networks:
  registry:
    name: registry
  